import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import { ThemeProvider } from '@/components/theme-provider';
import { Toaster } from '@/components/ui/sonner';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Ridwan Halim - AI/ML Enthusiast & Python Developer',
  description: 'Professional portfolio of Ridwan Halim - Machine Learning Mentor, Python Developer, and Open Source Contributor specializing in AI solutions.',
  keywords: ['AI', 'Machine Learning', 'Python', 'Data Science', 'Web Development', 'Portfolio'],
  authors: [{ name: 'Ridwan Halim' }],
  viewport: 'width=device-width, initial-scale=1',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <ThemeProvider
          attribute="class"
          defaultTheme="dark"
          enableSystem={false}
          disableTransitionOnChange
        >
          {children}
          <Toaster />
        </ThemeProvider>
      </body>
    </html>
  );
}